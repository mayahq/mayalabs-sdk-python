[project]
name = "mayalabs"
version = "0.0.1"
dependencies = [
    "requests",
    "pydantic",
    "websockets",
    "click",
    'importlib-metadata; python_version>"3.6"',
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

