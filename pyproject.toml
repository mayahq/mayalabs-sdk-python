[project]
name = "mayalabs"
version = "0.0.1"
dependencies = [
    "requests",
    "pydantic",
    "websockets",
    "click",
    "importlib-metadata",
    "colorama"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

